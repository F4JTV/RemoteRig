# Screenshots

Images referenced by the READMEs and by the HTML manuals. Keep them here so both
find them by the same relative path, and so `docs.qrc` can embed them.

Naming used by the documentation:

- `server-rig.png`, `server-audio.png`, `server-network.png` — the server's tabs
- `client-desktop.png` — the desktop client, main window
- `client-cw.png` — the desktop client's CW tab
- `client-android.png` — the touch client
- `client-android-cw.png` — the touch client's CW panel
